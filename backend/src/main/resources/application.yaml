spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://localhost:3306/tododb?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8
    username: todolist
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
